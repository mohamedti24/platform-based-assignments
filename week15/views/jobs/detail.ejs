<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Job Detail</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">

  <style>
    body {
      font-family: 'Inter', sans-serif;
      background: #f4f6f8;
      margin: 0;
      padding: 0;
      color: #333;
    }

    .container {
      max-width: 800px;
      margin: 60px auto;
      padding: 0 20px;
    }

    .job-card {
      background: #fff;
      border-radius: 14px;
      padding: 32px;
      box-shadow: 0 12px 30px rgba(0,0,0,0.08);
    }

    h1 {
      font-size: 2rem;
      margin-bottom: 10px;
      color: #111;
    }

    .job-meta {
      color: #6b7280;
      font-size: 0.95rem;
      margin-bottom: 24px;
    }

    h2 {
      font-size: 1.2rem;
      margin-top: 30px;
      margin-bottom: 12px;
      color: #111827;
    }

    ul {
      padding-left: 20px;
      margin: 0;
    }

    ul li {
      margin-bottom: 8px;
    }

    .actions {
      display: flex;
      gap: 12px;
      margin-top: 40px;
    }

    .btn {
      text-decoration: none;
      padding: 12px 20px;
      border-radius: 8px;
      font-weight: 600;
      transition: 0.2s ease;
    }

    .btn-primary {
      background: #2563eb;
      color: #fff;
    }

    .btn-primary:hover {
      background: #1e40af;
    }

    .btn-secondary {
      background: #e5e7eb;
      color: #111;
    }

    .btn-secondary:hover {
      background: #d1d5db;
    }
  </style>
</head>
<body>

  <div class="container">
    <div class="job-card">
      <h1><%= job.title %></h1>

      <div class="job-meta">
        Job ID: <%= id %> · Location: <%= job.location %> · Type: <%= job.type %>
      </div>

      <h2>Job Description</h2>
      <p><%= job.description %></p>

      <h2>Responsibilities</h2>
      <ul>
        <% job.responsibilities.forEach(item => { %>
          <li><%= item %></li>
        <% }) %>
      </ul>

      <h2>Requirements</h2>
      <ul>
        <% job.requirements.forEach(item => { %>
          <li><%= item %></li>
        <% }) %>
      </ul>

      <div class="actions">
        <a href="/jobs" class="btn btn-secondary">← Back to Jobs</a>
        <a href="/apply/<%= id %>" class="btn btn-primary">Apply Now</a>
      </div>
    </div>
  </div>

</body>
</html>
